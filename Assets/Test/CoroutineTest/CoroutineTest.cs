using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoroutineTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        NoMono noMono = new NoMono();
        noMono.Test();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
